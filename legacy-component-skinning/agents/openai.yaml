interface:
  display_name: "Legacy Component Skinning"
  short_description: "Reskin legacy UI, hidden surfaces, and error states"
  default_prompt: "Use $legacy-component-skinning when legacy UI must match a reference visual system without replacing components, including hidden interactive surfaces, degraded states, or skinning review."

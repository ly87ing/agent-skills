interface:
  display_name: "XYLINK Legacy Component Skinning"
  short_description: "Reskin legacy UI to XYLINK, including hidden and error states"
  default_prompt: "Use $xylink-legacy-component-skinning when legacy UI must align with XYLINK UIKit from demo URLs or screenshots without replacing components, including hidden interactive surfaces, degraded states, and acceptance review."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "XYLINK Legacy Component Skinning"
  short_description: "Reskin legacy UI to XYLINK, including hidden and error states"
  default_prompt: "Use $xylink-legacy-component-skinning when legacy UI must align with XYLINK UIKit without replacing components, including hidden interactive surfaces, degraded states, or XYLINK-specific skinning review."
policy:
  allow_implicit_invocation: true
